{
	"bin": {},
	"devDependencies": {
		"@joelek/ts-autoguard": "github:joelek/ts-autoguard#semver:^4",
		"@joelek/ts-sockets": "github:joelek/ts-sockets#semver:^2",
		"@types/node": "^14.14.6",
		"typescript": "^4.0.5",
		"webpack": "^5.3.2",
		"webpack-cli": "^4.1.0"
	},
	"dependencies": {},
	"license": "UNLICENSED",
	"main": "./dist/server.min.js",
	"name": "@joelek/castaway",
	"repository": {
		"type": "git",
		"url": "https://github.com/joelek/castaway.git"
	},
	"scripts": {
		"build": "rm -rf ./dist/ && npm run codegen && npx tsc && npm run bundle",
		"bundle:client": "npx webpack --entry ./build/client/client.js --output-filename client.min.js --target web",
		"bundle:server": "npx webpack --entry ./build/server/server.js --output-filename server.min.js --target node",
		"bundle": "npm run bundle:client && npm run bundle:server",
		"clean": "rm -rf ./build/ ./node_modules/",
		"codegen": "npx autoguard --standalone=false",
		"preversion": "npm run build && git add ./",
		"postversion": "git push && git push --tags"
	},
	"version": "0.0.88"
}
