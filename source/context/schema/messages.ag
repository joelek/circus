{
	SetContext: {
		context?: Context
	},

	SetDevice: {
		device?: Device
	},

	SetDevices: {
		devices: Device[]
	},

	SetIndex: {
		index?: number
	},

	SetLocalDevice: {
		device: Device
	},

	SetPlayback: {
		playback: boolean
	},

	SetProgress: {
		progress: number
	},

	SetToken: {
		token?: string
	}
}
