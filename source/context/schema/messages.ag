{
	SetContext: {
		context?: ./objects/Context
	},

	SetDevice: {
		device?: ./objects/Device
	},

	SetDevices: {
		devices: ./objects/Device[]
	},

	SetIndex: {
		index?: number
	},

	SetLocalDevice: {
		device: ./objects/Device
	},

	SetPlayback: {
		playback: boolean
	},

	SetProgress: {
		progress?: number
	},

	SetToken: {
		token?: string
	}
}
