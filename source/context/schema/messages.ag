{
	SetDeviceId: {
		id: string
	},

	Authenticate: {
		token: string
	},

	DeviceBecameAvailable: {
		device: Device
	},

	DeviceBecameUnavailable: {
		device: DeviceBase
	},

	AvailableDevices: {
		devices: Device[]
	},

	TransferPlayback: {
		device: DeviceBase
	},

	SetContext: {
		context?: Context
	},

	SetContextIndex: {
		index?: number
	},

	SetPlaying: {
		playing: boolean
	}
}
