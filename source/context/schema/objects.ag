{
	ContextAlbum: Album,

	ContextArtist: Artist,

	ContextTrack: Track,

	Context: ContextArtist | ContextAlbum,

	ContextItem: ContextTrack,

	Device: {
		id: string,
		type: string,
		name: string
	},

	Session: {
		context?: Context,
		device?: Device,
		index?: number,
		playback: boolean,
		progress?: number
	}
}
