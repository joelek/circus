{
	ContextAlbum: Album,

	ContextArtist: Artist,

	ContextTrack: Track,

	ContextPlaylist: Playlist,

	ContextMovie: Movie,

	ContextShow: Show,

	ContextSeason: Season,

	ContextEpisode: Episode,

	Context: ContextAlbum | ContextArtist | ContextTrack | ContextPlaylist | ContextMovie | ContextShow | ContextSeason | ContextEpisode,

	ContextItem: ContextTrack | ContextMovie | ContextEpisode,

	Device: {
		id: string,
		type: string,
		name: string
	},

	Session: {
		context?: Context,
		device?: Device,
		index?: number,
		playback: boolean,
		progress?: number
	}
}
