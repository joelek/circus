{
	EpisodeMetadata: {
		type: "episode",
		title: string,
		season: number,
		episode: number,
		year?: number,
		summary?: string,
		show: {
			title: string,
			summary?: string,
			genres: string[],
			actors: string[]
		}
	},

	MovieMetadata: {
		type: "movie",
		title: string,
		year?: number,
		summary?: string,
		genres: string[],
		actors: string[]
	},

	TrackMetadata: {
		type: "track",
		title: string,
		disc: number,
		track: number,
		album: {
			title: string,
			year?: number,
			artists: {
				title: string
			}[]
		},
		artists: {
			title: string
		}[]
	},

	Metadata: EpisodeMetadata | MovieMetadata | TrackMetadata,

	Probe: {
		metadata?: Metadata,
		resources: ../databases/media/Resource[]
	}
}
