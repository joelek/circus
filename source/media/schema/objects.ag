{
	ArtistBase: {
		artist_id: string,
		title: string
	},

	Artist: ArtistBase & {
		albums: Album[]
	},

	AlbumBase: {
		album_id: string,
		title: string,
		year: number,
		artists: ArtistBase[],
		artwork?: ImageFile
	},

	Album: AlbumBase & {
		discs: Disc[]
	},

	DiscBase: {
		disc_id: string,
		album: AlbumBase
	},

	Disc: DiscBase & {
		tracks: Track[]
	},

	TrackBase: {
		track_id: string,
		title: string,
		disc: DiscBase,
		artists: ArtistBase[],
		file: AudioFile,
		last_stream_date?: number
	},

	Track: TrackBase & {

	},

	MovieBase: {
		movie_id: string,
		title: string,
		year: number,
		summary: string,
		artwork?: ImageFile,
		file: VideoFile,
		subtitles: SubtitleFile[],
		last_stream_date?: number
	},

	Movie: MovieBase & {

	},

	ShowBase: {
		show_id: string,
		title: string
	},

	Show: ShowBase & {
		seasons: Season[]
	},

	SeasonBase: {
		season_id: string,
		number: number,
		show: ShowBase
	},

	Season: SeasonBase & {
		episodes: Episode[]
	},

	EpisodeBase: {
		episode_id: string,
		title: string,
		summary: string,
		number: number,
		file: VideoFile,
		subtitles: SubtitleFile[],
		season: SeasonBase,
		last_stream_date?: number
	},

	Episode: EpisodeBase & {

	},

	File: {
		file_id: string,
		mime: string
	},

	AudioFile: File & {
		duration_ms: number
	},

	ImageFile: File & {
		height: number,
		width: number
	},

	SubtitleFile: File & {
		language?: string
	},

	VideoFile: File & {
		duration_ms: number
	}
}
