{
	PersonBase: {
		person_id: string,
		name: string
	},

	Person: PersonBase & {

	},

	ArtistBase: {
		artist_id: string,
		title: string
	},

	Artist: ArtistBase & {
		albums: Album[]
	},

	AlbumBase: {
		album_id: string,
		title: string,
		year?: number,
		artists: ArtistBase[],
		artwork: ../../databases/media/ImageFile[]
	},

	Album: AlbumBase & {
		discs: Disc[]
	},

	DiscBase: {
		disc_id: string,
		album: AlbumBase,
		number: number
	},

	Disc: DiscBase & {
		tracks: Track[]
	},

	TrackBase: {
		track_id: string,
		title: string,
		disc: DiscBase,
		artists: ArtistBase[],
		number: number
	},

	Track: TrackBase & {
		last_stream_date?: number,
		media: ../../databases/media/AudioFile
	},

	UserBase: {
		user_id: string,
		name: string,
		username: string
	},

	User: UserBase & {

	},

	PlaylistBase: {
		playlist_id: string,
		title: string,
		description: string,
		user: UserBase
	},

	Playlist: PlaylistBase & {
		items: PlaylistItem[]
	},

	PlaylistItemBase: {
		number: number,
		playlist: PlaylistBase,
		track: Track
	},

	PlaylistItem: PlaylistItemBase & {

	},

	GenreBase: {
		genre_id: string,
		title: string
	},

	Genre: GenreBase & {

	},

	MovieBase: {
		movie_id: string,
		title: string,
		year?: number,
		summary?: string,
		artwork: ../../databases/media/ImageFile[],
		genres: Genre[],
		actors: Person[]
	},

	Movie: MovieBase & {
		last_stream_date?: number,
		media: ../../databases/media/VideoFile,
		subtitles: ../../databases/media/SubtitleFile[]
	},

	ShowBase: {
		show_id: string,
		title: string,
		summary?: string,
		artwork: ../../databases/media/ImageFile[],
		genres: Genre[],
		actors: Person[]
	},

	Show: ShowBase & {
		seasons: Season[]
	},

	SeasonBase: {
		season_id: string,
		number: number,
		show: ShowBase
	},

	Season: SeasonBase & {
		episodes: Episode[]
	},

	EpisodeBase: {
		episode_id: string,
		title: string,
		summary?: string,
		number: number,
		season: SeasonBase,
		year?: number
	},

	Episode: EpisodeBase & {
		last_stream_date?: number,
		media: ../../databases/media/VideoFile,
		subtitles: ../../databases/media/SubtitleFile[]
	},

	SubtitleBase: {
		subtitle_id: string,
		subtitle: ../../databases/media/SubtitleFile
	},

	Subtitle: SubtitleBase & {
		cues: Cue[]
	},

	CueBase: {
		cue_id: string,
		subtitle: SubtitleBase,
		start_ms: number,
		duration_ms: number,
		lines: string[]
	},

	Cue: CueBase & {

	},

	Entity: Album | Artist | Disc | Episode | Genre | Movie | Person | Playlist | Season | Show | Track | User
}
